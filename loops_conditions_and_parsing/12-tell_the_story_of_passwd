#!/usr/bin/env bash
# 100% working /etc/passwd display script

while IFS=: read -r a b c d e f g; do
    echo "The user $a is part of the $d gang, lives in $f and rides $g."
    echo "$a's place is protected by the passcode $b, more info about the user here: $e"
    echo
done < /etc/passwd

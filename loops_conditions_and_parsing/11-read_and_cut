#!/usr/bin/env bash
# This script displays the username, user id, and home directory path for each user in /etc/passwd.

while read -r username _ user_id _ _ home_dir _; do
    # Only print the required fields
    echo "$username $user_id $home_dir"
done < /etc/passwd
